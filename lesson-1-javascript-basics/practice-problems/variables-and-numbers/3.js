// Declare a variable named answer and set it to the result of numerator
// divided by DENOMINATOR. Log the value of answer (it should be 5).

let numerator = 10;
const DENOMINATOR = 2;

let answer = numerator / DENOMINATOR;

console.log(answer);
